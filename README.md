# life

A private, personal tracker at [life.ammaarkhan.com](https://life.ammaarkhan.com). Plain HTML/CSS/JS on GitHub Pages.

This repo is only the shell: all content and data load at runtime from a private repo via the GitHub API, so the page renders nothing without a key.
